/**
 * Shared layout constants for Chat ↔ Terminal symmetry.
 * When both panes are visible in split view, headers, input bars,
 * and shortcut bars should align at the same vertical position.
 */

/** Header bar height (both Chat and Terminal) */
export const HEADER_HEIGHT = 40;

/** Shortcut key bar / action bar height (matches CommandKeyBar.container.minHeight) */
export const KEY_BAR_HEIGHT = 52;

/** Header horizontal padding */
export const HEADER_PADDING_H = 12;

/** Border width shared across headers/bars */
export const BORDER_WIDTH = 1;
