# 2026-04-22 Session Handoff

Continuation of 2026-04-21. Two tracks this session: bug #116 body-tap
focus regression on-device verification, and Termux-host claude-code
runtime resurrection.

## Current state

Branch: `main`, tip `ebb69e98` pushed.
Latest APK build started: run `24781302533`, artifact `shelly-apk`
will contain commit `ebb69e98` once complete. **Not yet verified
on-device.**

Follow-up commits after the original handoff:

- `7a3b49c6` fix(terminal): restart IME input on pane focus
- `32a0cde8` feat(multi-pane): highlight focused pane and color session tabs
- `eafb01b1` fix(terminal): keep wrapper background aligned with theme
- `b61f2d2a` fix(multi-pane): prevent duplicate terminal session mounts
- `772310e8` fix(browser): avoid pausing retained WebView media
- `ebb69e98` feat(browser): adapt chrome density to pane size

## Track 1 — bug #116 body-tap focus (ShellyTerminalView IME handoff)

### Timeline of commits on main

- `e85694a3` fix(multi-pane): route keyboard input to the tapped pane — RN-level handleFocusPane
- `e13d6651` fix(multi-pane): bind terminal session per slot at init
- `6bff63f5` fix(multi-pane): close dup sessionId + disableMultiPane gaps
- `49d25de3` fix(multi-pane): per-pane session tab switching
- **`70e95340`** fix(multi-pane): native onFocusRequested event for body-tap focus handoff ← this session
- **`30dc9d02`** diag+fix: IME refuses to open for non-left pane ← this session, **under verification**

### What the review agent found (commit 70e95340 rationale)

The four pre-session commits all assumed `handleFocusPane` in `PaneSlot`
ran on every pane tap. It does not: `ShellyTerminalView.dispatchTouchEvent`
(at `modules/terminal-view/.../ShellyTerminalView.kt:171-176`) calls
`parent?.requestDisallowInterceptTouchEvent(true)`, which prevents RN's
parent `<View onTouchStart=...>` from receiving the gesture. So body
taps never updated the 4 focus stores; only header/tab taps did.

Fix in `70e95340` — emit a new native event `onFocusRequested` from
`onSingleTapUp` carrying `sessionId`, subscribe in `TerminalPane` and
mirror the 4-store handoff. Files:
- `modules/terminal-view/android/.../ShellyTerminalView.kt`
- `modules/terminal-view/android/.../TerminalViewModule.kt`
- `modules/terminal-view/src/NativeTerminalView.tsx`
- `components/panes/TerminalPane.tsx`

### On-device verification (24722846313 build, commit 70e95340)

User installed, tested. Logcat via ADB Wireless (paired device
`192.168.1.6:43717`) showed:

- `onSingleTapUp: emitting onFocusRequested sessionId=shelly-2` ✓
- `[Shelly][Pane] onFocusRequested paneId=pane-mo99bdcu-3 sessId=shelly-2` ✓
- `paneId` and `sessId` resolve to the correct tapped pane.

**But**: user reported the right pane still does not receive keyboard
input. The IME does not open on right-pane body tap. Controlled test:

- Right-pane body tap 7× (08:37:13–22): onFocusRequested fires, **zero
  didShow**.
- Left-pane tap (08:37:23): onFocusRequested + **immediate didShow**.
- Right-pane tap again: onFocusRequested, **no didShow**.

User's hypothesis (confirmed by log): once the left pane gains focus,
no subsequent right-pane tap can re-open the IME.

### Speculative fix + diagnostics in 30dc9d02

`onSingleTapUp` was:
```kotlin
terminalView.requestFocus()
imm?.showSoftInput(terminalView, 0)
```

Replaced with:
```kotlin
terminalView.isFocusable = true
terminalView.isFocusableInTouchMode = true
val rootFocused = rootView?.findFocus()
if (rootFocused != null && rootFocused !== terminalView) rootFocused.clearFocus()
val reqOk = terminalView.requestFocusFromTouch()
// ...
val imeShown = imm?.showSoftInput(terminalView, InputMethodManager.SHOW_IMPLICIT) ?: false
Log.i(TAG, "onSingleTapUp sess=$sid reqFocus=$reqOk hasWin=$hasWinFocus focused=$isFocused ...")
```

Three compounded speculative changes:
1. Force `isFocusable + isFocusableInTouchMode` before requesting.
2. Explicit `clearFocus()` on whatever sibling currently owns focus.
3. `requestFocusFromTouch()` + `SHOW_IMPLICIT` flag.

Plus one `Log.i` per tap with requestFocus result, hasWindowFocus,
isFocused, focusable flags, visibility, width/height, showSoftInput
return, previous-focus-owner's class+hash, and current view's hash.

If the speculative fix works: bug closes, remove the noisy log in a
follow-up commit.
If it doesn't work: the log tells us which layer is refusing — focus
refusal, no window focus, IME returning false, or sibling still
holding focus after clearFocus.

### Build artifact for verification

- Current run: https://github.com/RYOITABASHI/Shelly/actions/runs/24781302533
- Artifact: `shelly-apk` (commit `ebb69e98`, once complete)
- Install → 2-pane → keyboard closed → tap right pane body
- `adb logcat -v time ShellyTerminalView:I ReactNativeJS:I *:S`
- Look for `onSingleTapUp sess=shelly-2 ... imeShow=...` vs
  `[Shelly][Keyboard] didShow` pairing

### Follow-up diagnosis after user reproduced on latest APK

ADB wireless was connected to `SM_F956Q`
(`adb-RFCX71399SK-K0piCb._adb-tls-connect._tcp`). `scrcpy` was installed
via winget and launched as `Shelly-Device`.

Captured logcat showed the critical failure:

```text
Ignoring showSoftInput() as view=com.termux.view.TerminalView{3652cbf ...} is not served.
servedView=com.termux.view.TerminalView{2939ab8 ...}
onSingleTapUp sess=shelly-1 reqFocus=true hasWin=true focused=true ... imeShow=false
```

Interpretation: native focus moved to the tapped right-pane
`TerminalView`, but Android IME's served view stayed bound to the first
`TerminalView`. So `showSoftInput()` was rejected at
`PHASE_CLIENT_VIEW_SERVED`.

Fix in `7a3b49c6`:

- `focusCommand()` now uses `requestFocusFromTouch()`.
- Both `focusCommand()` and `onSingleTapUp()` call
  `InputMethodManager.restartInput(terminalView)` before `showSoftInput()`.
- A next-frame retry calls `showSoftInput()` again and logs
  `onSingleTapUp.retry ... imeShow=...`.

Verification target after installing `ebb69e98` APK:

- Tap left/right terminal pane bodies alternately.
- Confirm `onSingleTapUp.retry ... imeShow=true` or at least
  `[Shelly][Keyboard] didShow` follows right-pane taps.
- Confirm typed text lands in the tapped pane's session.

## Track 3 — multi-pane terminal UX follow-ups

### Focused pane visibility

User asked whether the currently selected pane is visible. Before this
session the stores tracked it (`usePaneStore.focusedPaneId`,
`useMultiPaneStore.focusedSlot`, terminal active session), but chrome did
not make it obvious.

Fix in `32a0cde8`:

- Focused pane gets an accent border.
- Focused header gets tinted background and stronger bottom border.
- Focused header gets a 3px accent rail at the left.
- Pane type pill/label become accent-tinted when focused.

### Session tab colour coding

`PaneCliTabs` already had stable per-session hues, but inactive tabs were
too faint.

Fix in `32a0cde8`:

- Every session tab now shows its stable hue even when inactive.
- Active tab gets stronger fill, border, and bottom accent line.
- Close icon inherits the session hue.

### Terminal wrapper grey flash

User observed slight grey tint/background when switching session tabs.
No code intentionally changes terminal background per session. Likely
cause: RN wrapper/container used theme `c.background`, which can differ
from the native terminal theme background and briefly show through during
tab rebind/repaint.

Fix in `eafb01b1`:

- `TerminalPane` outer container background now uses
  `terminalColorScheme.background`.
- `NativeTerminalView` wrapper style also uses
  `terminalColorScheme.background`.

### Duplicate session tab mounted in multiple panes

User reported layout breakage when opening the same terminal session tab
in multiple panes. Root cause is native architecture: one
`ShellyTerminalSession` owns one shared `TerminalSession.emulator`, size
state, PTY resize, and one `onScreenUpdateCallback`. Attaching that same
session to multiple `TerminalView`s makes them fight over:

- emulator rows/cols via `TerminalView.updateSize()`
- PTY resize via `ShellyTerminalSession.resize()`
- redraw callback assignment (`onScreenUpdateCallback` is overwritten)

Fix in `b61f2d2a`:

- If a user selects a session already visible in another terminal pane,
  do not mount it twice. Swap the two panes' `slot.sessionId` bindings
  instead.
- Closing a session rebinds every terminal pane that still references it
  to the first remaining session.

Verification target:

- Open 2-4 terminal panes.
- Try selecting the same session tab in pane 2 while pane 1 owns it.
- Expected: pane 1 and pane 2 swap sessions; no duplicate rendering, no
  layout corruption.

## Track 4 — Browser pane fixes

### YouTube/video stopping frequently

BrowserPane had a `visible` effect that injected JS to pause all playing
videos when hidden and play them again when shown. Because BrowserPane is
kept mounted in hidden mode after first opening, pane switches/type
changes could repeatedly call `video.pause()` / `video.play()`.

This is hostile to YouTube's player state: play promises can be rejected,
ads/player state can desync, and autoplay policies can treat resume as
not user-initiated.

Fix in `772310e8`:

- Removed the hidden-pane pause/resume JS injection.
- WebView remains mounted and page media state is left to the page.

### Browser pane density in 4-pane layouts

User reported 4-pane browser was too large/chrome-heavy, making text
input hard. The fixed-size URL bar, bookmark bar, and bottom input bar
consumed too much of a small pane.

Fix in `ebb69e98`:

- BrowserPane reads `MultiPaneContext` pane width/height.
- Compact mode when width `< 430` or height `< 380`:
  - URL toolbar padding/icon/input sizes shrink.
  - Bookmark bar becomes icon-only.
  - Bottom `PaneInputBar` is hidden.
  - WebView `textZoom` becomes `85`.
- Tiny mode when width `< 320` or height `< 300`:
  - Bookmark bar is hidden entirely.

Verification target:

- p4 layout with Browser + Terminal panes.
- Confirm browser chrome is compact and text input in neighboring panes
  remains practical.
- Confirm YouTube still plays while switching focus/panes.

## Track 5 — public GitHub feed check

User asked whether there was reaction in the GitHub feed. Public API
showed:

- Stars: 5
- Watchers: 5
- Forks: 2
- Open issues/PRs: 19

Most recent visible external reaction was issue #24:
https://github.com/RYOITABASHI/Shelly/issues/24

Issue #24 by `PKailab` is positive feedback plus a feature request for
ready-made project/template files (WebView, React Native TypeScript,
JavaScript, screenshot templates, etc.), referencing:
https://github.com/AndroidCSOfficial/android-code-studio

`gh` is not authenticated on this machine, so personal GitHub
notifications were not checked; only public repo feed/issues metadata.

## Track 2 — Termux-host claude-code resurrection

Claude-code 2.1.113+ ships as a Bun SEA binary with ELF interpreter
`/lib/ld-musl-aarch64.so.1` that Android bionic does not provide. The
npm-installed claude was hitting the postinstall stub that prints
"native binary not installed" and exiting 1.

### Actions taken (Termux host only, Shelly APK untouched)

1. Located the musl loader from prior bug #117 exploration:
   `/data/data/com.termux/files/usr/tmp/bun-sea-test/alpine/lib/ld-musl-aarch64.so.1`
2. Copied to stable path: `$PREFIX/share/shelly-loader/ld-musl-aarch64.so.1`
3. Replaced `$PREFIX/lib/node_modules/@anthropic-ai/claude-code/bin/claude.exe`
   with a wrapper that invokes `env -u LD_PRELOAD $LOADER $BIN "$@"`.
   (`LD_PRELOAD=libtermux-exec-ld-preload.so` is cleared because the
   bionic-targeted shim fails symbol resolution under musl.)
4. Edited `~/.bashrc` — removed the old `alias claude="TMPDIR=... claude-cli"`
   line (CJS-era artifact) and the related `claude-fix` alias.

Result: `claude --version` → `2.1.114 (Claude Code)` in a fresh bash.
The `cli.js not found` error was a stale shell alias, not a broken
install.

### What remains broken on Termux

- **claude /login and codex login**: OAuth browser flow cannot complete
  in Termux (no working xdg-open under musl for the SEA binary).
  Standing workaround: credentials transplant from a PC/browser device.
  See `2026-04-20-claude-credentials-transplant.md`.
- Options discussed but not yet chosen:
  - A. Use `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` — separate billing.
  - B. OAuth credentials transplant from another device — subscription
       passes through but refresh-token expiry forces re-transplant.
  - C. Manual OAuth URL copy/paste flow — to be tried for codex.

### Gemini — no change

`gemini` is unaffected by the Bun SEA transition; still runs under node.

## Shelly-PC-side context (unchanged from 2026-04-21)

bug #117 Path C (custom musl libc with patched `/etc/resolv.conf`) is
still the path of record for running the *latest* claude-code on-device.
The in-session workaround above uses the 2.1.114 musl binary that
already shipped in the node_modules tarball — it just wasn't being
invoked because the wrapper defaulted to the CJS stub.

Handoff doc for the full libc rebuild is still
`2026-04-21-bug117-path-c-handoff.md`. Nothing in this session
supersedes it.

## Next-session entry point

1. Wait for / install APK from the latest run for `main`. As of the
   latest update, run `24808373116` is building commit `f970a686`:
   https://github.com/RYOITABASHI/Shelly/actions/runs/24808373116.
2. Verify terminal multi-pane:
   - left/right body tap opens IME
   - typing lands in tapped pane
   - focused pane chrome is obvious
   - session tab colours are readable
   - selecting an already-visible session swaps panes instead of duplicating
3. Verify Browser pane:
   - YouTube does not pause on pane focus/layout changes
   - p4 browser chrome compacts correctly
   - tiny browser pane remains usable
4. If IME fix works, remove or downgrade noisy
   `onSingleTapUp.retry` diagnostic log.
5. Termux OAuth: pick A/B/C and execute.
6. Resume bug #117 custom musl libc on PC only when UI regressions above
   are verified.

## Files changed this session

- `modules/terminal-view/android/.../ShellyTerminalView.kt` — onFocusRequested dispatcher, diag+speculative fix in onSingleTapUp
- `modules/terminal-view/android/.../TerminalViewModule.kt` — Events registration
- `modules/terminal-view/src/NativeTerminalView.tsx` — FocusRequestedEvent type + prop
- `components/panes/TerminalPane.tsx` — onFocusRequested handler with 4-store handoff
- `components/multi-pane/PaneSlot.tsx` — focused pane chrome
- `components/multi-pane/PaneCliTabs.tsx` — session tab colours + duplicate-session swap guard
- `components/panes/TerminalPane.tsx` — terminal wrapper background follows terminal theme
- `components/panes/BrowserPane.tsx` — removed video pause injection; added responsive browser chrome
- `~/.bashrc` (Termux host) — removed dead `alias claude` + `alias claude-fix`
- `$PREFIX/lib/node_modules/@anthropic-ai/claude-code/bin/claude.exe` (Termux host) — Bun SEA musl loader wrapper
- `$PREFIX/share/shelly-loader/ld-musl-aarch64.so.1` (Termux host) — vendored musl loader

## 2026-04-23 addendum — AI CLI input / paste / shell execution

User installed the APK from run `24784117104` (`277a8248`) and verified
that multi-pane text input was much improved. On-device state before the
new fixes:

- Two terminal panes open.
- Left pane: Claude Code running.
- Right pane: second shell, then Codex, then Gemini.
- IME served view correctly moved to the right pane when tapped:
  `mServedView=com.termux.view.TerminalView{af3c89b ... 0,0-669,894}`.

### Confirmed working on current installed APK

- Right-pane normal shell input worked:
  `echo pane2-test` landed in the right pane only and printed
  `pane2-test`.
- Codex TUI launched as:
  `/data/user/0/dev.shelly.terminal/files/home/.shelly-runtime/codex/current/codex_tui`.
- Codex one-line paste worked.
- Codex multi-line paste worked. Log:

```text
ShellyPaste: paste(raw=39, sanitized=38, nl=1, bracketed=true,
preview=" paste-test line 1↵  paste-test …")
```

- Gemini multi-line paste also worked. Log:

```text
ShellyPaste: paste(raw=38, sanitized=37, nl=1, bracketed=true,
preview="paste-test line 1↵  paste-test l…")
```

### Confirmed broken on current installed APK

Claude Code Bash tool failed every command, including `pwd`, `echo`,
`git --version`, and `true`, with `Error: Exit code 1`. Process tree
showed Claude running through the musl trampoline:

```text
/system/bin/linker64 .../shelly_musl_exec .../ld-musl-aarch64.so.1 .../.shelly-runtime/claude/current/claude
```

Root cause: Claude's tool execution spawns a shell outside Shelly's
interactive bash functions. `$SHELL` pointed at an app-data `libbash.so`,
which is not directly execable under Android targetSdk noexec policy.

Codex internal command execution also failed:

```text
Ran echo codex-bash-test
error: unable to open file "/bin/sh"
```

Root cause: Codex uses a Linux-native `/bin/sh` path. Android provides
`/system/bin/sh`, not `/bin/sh`.

Gemini internal command execution produced long Gemini-side reasoning/log
output, not an app crash. It said it could not run `bash`, had no
`run_node_code`, and tried delegating to a `generalist` agent. Root cause
is likely that Gemini's Node process calls `spawn("bash", ...)`, which
cannot see exported bash functions from `.bashrc`; it needs a real
PATH-visible executable.

### Fixes pushed after those tests

- `5a2c617f` `fix(terminal): restore claude shell tools and paste`
  - Added `modules/terminal-emulator/android/src/main/jni/shelly-shell-launcher.c`.
  - CI builds it as `libshelly_shell.so`.
  - `.bashrc` v49 sets:
    - `SHELLY_LIB_DIR=$libDir`
    - `SHELL=$nativeLibDir/libshelly_shell.so`
  - The launcher lives in APK nativeLibraryDir, so it is directly
    executable, then execs `/system/bin/linker64 $SHELLY_LIB_DIR/libbash.so`.
  - It injects `LD_PRELOAD=$SHELLY_LIB_DIR/libexec_wrapper.so` into the
    child bash so app-data binaries still flow through linker64.
  - `TerminalEmulator.paste()` now sends standard bracketed paste
    `ESC[200~ ... ESC[201~` when the guest is TUI-like
    (`alternateBuffer || mouseTracking`), while keeping Shelly's
    `C-x C-b` readline workaround for normal bash prompts.

- `d8ebb68f` `fix(terminal): keep pane background stable`
  - Prevents terminal apps from changing Shelly's pane background via OSC
    11 default-background color changes. Claude Code had visibly tinted
    the active pane grey.
  - Extends Samsung/One UI IME retry window in
    `ShellyTerminalView.showKeyboardWhenServed()` to `0/50/150/300/600ms`.

- `6b80e536` `fix(codex): map /bin/sh to android shell`
  - `libexec_wrapper.so` now rewrites `execve("/bin/sh", ...)` to
    `/system/bin/sh`.
  - Intended to fix Codex's internal command execution path.

- `f970a686` `fix(gemini): expose path-visible bash`
  - `.bashrc` v50 exports `BASH=$SHELL`.
  - `.bashrc` creates/refreshes `~/bin/bash -> $SHELL`.
  - Intended to fix Node-based CLIs such as Gemini that call
    `spawn("bash", ...)` and cannot see exported bash functions.

### Current latest build

Latest pushed tip: `f970a686`.

Latest Actions run at handoff time:
https://github.com/RYOITABASHI/Shelly/actions/runs/24808373116

Status at last check: `in_progress`.

Older runs for intermediate commits were also still in progress at the
time:

- `24808170057` for `6b80e536`
- `24807822493` for `d8ebb68f`
- `24807559691` for `5a2c617f`

### Next APK verification checklist

Install the APK built from `f970a686`, then verify:

1. Multi-pane input
   - tap left/right terminal bodies alternately
   - keyboard opens
   - typed text lands only in the tapped pane

2. Background stability
   - launch Claude Code in one pane
   - confirm the terminal canvas does not turn grey from OSC 11
   - focused pane should be visible through border/header chrome, not by
     tinting the terminal background

3. Paste
   - in Claude Code, Codex, and Gemini, paste:

```text
paste-test line 1
paste-test line 2
```

   - expected: text appears in the TUI input box as a paste block; no
     literal `[200~` / `[201~` leakage.

4. Internal shell/Bash execution
   - Claude Code prompt:

```text
Bashで echo claude-bash-test を実行して、結果だけ表示して
```

   - Codex prompt:

```text
Bashで echo codex-bash-test を実行して、結果だけ表示して
```

   - Gemini prompt:

```text
Bashで echo gemini-bash-test を実行して、結果だけ表示して
```

   - expected: each returns only the matching `*-bash-test` output or a
     normal concise tool result. No `Exit code 1`, no `/bin/sh`, no
     `bash not found`.

5. Baseline shell toolchain from a normal shell pane:

```sh
echo test
pwd
ls
git --version
python3 --version
node --version
mktemp
which env
ls -l /usr/bin/env /bin/sh /system/bin/sh
```

Known likely future risk: `/usr/bin/env` and `/tmp` hard-coded paths may
still need explicit handling if an AI CLI or package script uses them.

## 2026-04-23 late addendum — HN stabilization pass

User goal: ship a credible Hacker News post today. Priority is now
stability over broader redesign. Do not introduce a new terminal input
model before HN unless absolutely required; the inline-input idea would
expand the test surface across IME composition, Japanese input, paste,
TUI focus, and PTY dispatch. Current bugs are layout/session-management
bugs, not a fundamental need to replace native inline terminal input.

### Commits after the previous AI CLI handoff

- `49ebc3d4` `fix(terminal): harden AI CLI paste and shell spawn`
  - Keeps the native ClipboardManager paste path:
    `CommandKeyBar -> TerminalEmulator.pasteClipboardToSession()`.
  - Adds `ShellyPaste` log for native clipboard paste length.
  - Extends `libexec_wrapper.so` beyond `execve()`:
    - maps `/bin/sh -> /system/bin/sh`
    - maps `/usr/bin/env -> /system/bin/env`
    - maps `/bin/bash -> $SHELL`
    - intercepts `posix_spawn()` as well as `execve()`
  - `.bashrc` v51 adds PATH-visible `~/bin/sh` and `~/bin/env` links.
  - Also fixes native transparent-background reset to use the active
    terminal theme background instead of hard-coded black.

- `a58aa8b8` `fix(multipane): keep terminal above android keyboard`
  - Added Android `Keyboard.metrics()` polling fallback because Samsung
    keyboard can show IME even when RN event timing is unreliable.
  - This did **not** solve the visible keybar issue by itself. Logs showed
    `keyboardDidShow` and metrics were present, so the issue was not height
    detection.

- `85c2be60` `fix(terminal): pin command key bar above keyboard`
  - Moves `CommandKeyBar` into an absolute bottom dock inside
    `TerminalPane`.
  - Gives the terminal body `KEY_BAR_HEIGHT` bottom padding so native
    terminal content cannot consume the keybar area.
  - This is intended to make Paste/Ctrl/Tab controls visible above the
    keyboard.

- `bb406e22` `fix(multipane): preserve panes when switching layouts`
  - Root cause of "multi-pane is dead": `setPreset(p1)` was destructively
    trimming surplus slots and destroying sessions.
  - New behavior: smaller presets hide surplus panes without deleting
    slots or terminal sessions. Returning to p2/p3/p4 restores them.

- `8470db9b` `fix(multipane): label hidden panes without deletion`
  - Updates LayoutPicker copy from destructive `(-N)` trimming semantics
    to non-destructive `(capacity/used)` hidden-pane semantics.

### Latest build to install

Use the newest run for `8470db9b`, not the intermediate runs:

- Latest run observed: https://github.com/RYOITABASHI/Shelly/actions/runs/24834544975
- Status when this note was written: `in_progress`
- Older in-progress runs (`85c2be60`, `bb406e22`) are superseded and
  should not be installed for HN testing.

### On-device observations that drove the fixes

Installed APK before `85c2be60`/`bb406e22`:

- `lastUpdateTime=2026-04-23 21:04:15`
- PID `25006`
- IME target was correct:

```text
mServedView=com.termux.view.TerminalView{...}
mServedInputConnection=com.termux.view.TerminalView$2...
```

- Keyboard metrics existed:

```text
[Shelly][Keyboard] didShow raw=280.3448 adjusted=265.1724
[Shelly][Keyboard] syncMetrics adjusted=280.3448
```

Therefore the keybar problem was not missing keyboard events; the
TerminalPane layout was not keeping `CommandKeyBar` visible.

MultiPane failure log:

```text
[Shelly][MultiPane] setPreset p1 — trimmed 2 surplus pane(s): ...
```

This confirmed that layout switching deleted panes instead of hiding
them.

### HN test checklist after installing `8470db9b` APK

Run these in this order. Stop at the first failure and capture a
screenshot plus logcat.

1. Basic terminal startup
   - open Shelly fresh
   - terminal prompt appears
   - tap terminal body
   - keyboard opens
   - `CommandKeyBar` / Paste row remains visible above keyboard

2. MultiPane layout safety
   - open 2 panes, then 4 panes
   - switch layout to p1
   - switch back to p4
   - expected: prior panes/sessions are restored, not deleted

3. MultiPane focus/input
   - tap each pane body
   - keyboard opens
   - text lands only in tapped pane

4. Claude Code paste
   - launch `claude`
   - paste one short line with Paste button
   - expected log includes `ShellyPaste pasteClipboardToSession ... len>0`
   - if no `ShellyPaste` log appears, the button press is still not
     reaching native paste

5. AI CLI shell execution
   - Claude: ask it to run `echo claude-bash-test`
   - Codex: ask it to run `echo codex-bash-test`
   - Gemini: ask it to run `echo gemini-bash-test`
   - Watch for `ShellyExecWrapper` logs if a tool fails.

### Useful log commands

```powershell
adb logcat -c
adb logcat -d -v time --pid=<PID> | findstr /i "ShellyPaste ShellyExecWrapper Keyboard MultiPane ShellyTerminalView ReactNativeJS"
adb shell dumpsys input_method | findstr /i "mInputShown mServedView mNextServedView mServedInputConnection"
adb shell dumpsys package dev.shelly.terminal | findstr /i "versionCode versionName lastUpdateTime"
```

## 2026-04-24 Build / Device Mismatch Handoff

### Latest local fix commit

- `b59d215d` `fix(terminal): restore paste shell and prompt visibility`
  - `TerminalEmulator.paste()`:
    - when bracketed paste mode is on, multiline paste now uses standard
      bracketed-paste markers even if `tui=false`
  - `exec-wrapper.c`:
    - now intercepts `execvp`, `execvpe`, and `posix_spawnp`
    - rewrites basename-only `sh`, `env`, and `bash` to Android-visible
      executables
  - `TerminalPane.tsx`:
    - adds extra bottom inset to the native terminal view so the prompt
      should not disappear under the bottom bar / keyboard edge

### What the device logs proved

Claude paste is reaching native code. The remaining failure is not the
Paste button path itself.

Observed on-device logs:

```text
D/ShellyPaste: pasteClipboardToSession session=shelly-1 len=21
D/ShellyPaste: paste(raw=21, sanitized=21, nl=0, bracketed=true, tui=false, preview="echo claude-bash-test")
```

Also confirmed at the same time:

```text
mServedView=com.termux.view.TerminalView{...}
mServedInputConnection=com.termux.view.TerminalView$2...
```

So the current diagnosis is:

- `CommandKeyBar` exists and button presses reach native paste
- clipboard text is being read correctly
- `TerminalEmulator.paste()` is being called
- the remaining Claude failure is the paste format branch, or the device
  APK not actually containing `b59d215d`

### Codex / Gemini shell status before re-test

Codex user-visible result before `b59d215d` was verified:

```text
error: unable to open file "/bin/sh"
```

Then Codex retried with its own fallback shell and succeeded. That means
the first-run shell resolution path was still broken even though the CLI
appeared to recover.

Gemini was much healthier:

```text
/data/data/dev.shelly.terminal/files/home
gemini-bash-test
/data/data/dev.shelly.terminal/files/home/bin/sh
/usr/bin/env
```

### Suspected build mismatch

The user reported downloading the newest APK from GitHub, but the device
state still looked older than the latest local fix.

Observed on device:

```text
versionName=4.2.0
lastUpdateTime=2026-04-24 08:34:05
```

At that point the log behavior still matched the pre-`b59d215d` paste
branch. Therefore one of these is true:

1. the Actions run for `b59d215d` did not produce the expected APK
2. the user installed an older artifact
3. the artifact/build labeling does not make the commit provenance obvious

### First checks in any new environment

Before debugging code further, confirm the build provenance:

1. verify which GitHub Actions run built commit `b59d215d`
2. verify the downloaded APK/artifact belongs to that exact commit
3. install that APK
4. re-run these tests:
   - Claude Paste button
   - Claude shell test: `echo claude-bash-test`
   - Codex shell test: `echo codex-bash-test`
   - terminal prompt visibility at the bottom edge

### If the same failures remain after a verified `b59d215d` install

Then continue in this order:

1. Claude paste formatting
   - inspect whether the guest CLI expects fully standard bracketed paste
     for single-line content too, not only multiline content
2. Codex shell spawn
   - inspect whether Codex is bypassing the covered spawn/exec entry points
3. prompt overlap
   - inspect native `TerminalView` height vs. RN layout height after IME
     shows, especially in multi-pane mode

## 2026-04-24 afternoon — Bash-tool / paste / IME stabilization

Worktree: `.claude/worktrees/friendly-goldwasser-392b6f` on
`claude/friendly-goldwasser-392b6f`.

On-device diagnosis of the latest installed APK (versionName=4.2.0,
lastUpdateTime 2026-04-24 08:34:05) plus logcat from Claude / Codex /
Gemini Bash-tool tests located the real causes of the AI-CLI shell
failures and the "Enter after paste does nothing" regression. Six fix
commits landed:

| # | Commit | Area | Fix |
|---|--------|------|-----|
| 1 | `5dc83637` | `lib/layout-constants`, `CommandKeyBar` | `KEY_BAR_HEIGHT 32 -> 52` so the bar stops covering the bottom prompt row (~11 px). |
| 2 | `f5246f46` | `HomeInitializer` v52 | Re-point `$SHELL` at `$HOME/bin/bash` after the symlink is created. `basename($SHELL)` now reads `bash` so Claude Code CLI's shell-name validation accepts it. |
| 3 | `ecef1cb6` | `TerminalEmulator.paste()` | Drop the `multiLine` branch from `b59d215d`. Multi-line pastes on plain bash were being routed through the standard `\e[200~/\e[201~` path; bionic bash 5.3 eats the ESC at readline dispatch, so "[200~" leaked and each pasted line ran as its own (broken) command. Fall back to the `\C-x\C-b + \e[201~` path for non-TUI guests, which `rl_read_key`-loops until the end marker and handles multi-line correctly. |
| 4 | `8ec67af4` | `LibExtractor`, `HomeInitializer` v53 | Extract `libshelly_shell.so` as `$libDir/shelly_shell` and point `$SHELL` there. The `nativeLibraryDir` path was empty on zero-copy-packaged installs and changed obfuscation name every APK reinstall, leaving `$HOME/bin/bash` as a dangling symlink. Net effect before: Claude / Gemini / Codex all hit `spawn bash ENOENT` the moment they ran a shell tool. |
| 5 | `7d31e45a` | `exec-wrapper.c` | Add generic `/bin/* -> /system/bin/*` and `/usr/bin/* -> /system/bin/*` rewrites (kept after the specific `sh`/`bash`/`env` cases). Codex-termux's sandbox hard-codes `/bin/whoami`, `/bin/uname`, `/bin/id`, etc. — they now route to toybox at `/system/bin/`. |
| 6 | `c236349f` | `ShellyTerminalView.showKeyboardWhenServed()` | Drop the five-attempt postDelayed retry ladder (0/50/150/300/600 ms). Android 15+ edge-to-edge rejects every postDelayed attempt with `fromUser=false` at `PHASE_CLIENT_APPLY_ANIMATION`. Because both `onSingleTapUp` and `TerminalPane.focusedPaneId` effect (via `TerminalViewModule.focus`) reach this method for the same tap, a single gesture spawned up to 10 cancelled show requests that timed out. Single synchronous attempt + `WindowInsetsController.show(ime())` fallback. |

### On-device evidence captured this session

- Symlink dangling after reinstall:
  ```text
  $ ls -la $HOME/bin/bash
  lrwxrwxrwx ... -> '/data/app/~~P3BCh.../lib/arm64/libshelly_shell.so'
  $ file $HOME/bin/bash
  broken symbolic link
  $ pm path dev.shelly.terminal   # current install
  /data/app/~~p9OyC.../base.apk
  $ ls /data/app/~~p9OyC.../lib/arm64/
  (empty)
  ```
  Confirms two independent breakages on the v49/v50 design: nativeLibDir
  is empty on this install, and the symlink target was baked with the
  previous install's obfuscated path.

- Paste Enter failure logcat (pre fix #3):
  ```text
  ShellyPaste: paste(raw=198, sanitized=195, nl=3, bracketed=true, tui=false, ...)
  ```
  Matches the `b59d215d` broken branch that routed non-TUI multi-line
  through `\e[200~`.

- IME retry storm (pre fix #6):
  ```text
  ImeTracker onRequestShow at ORIGIN_CLIENT reason SHOW_SOFT_INPUT fromUser false
  ImeTracker onCancelled at PHASE_CLIENT_APPLY_ANIMATION
  (x ~10, interleaved onSingleTapUp.keyboardRetry and focusCommand.keyboardRetry)
  ```

### What still needs on-device verification after the next APK build

Run in order. Stop at the first failure and capture logcat
(`ShellyPaste`, `ShellyExec`, `ShellyExecWrapper`, `HomeInitializer`,
`ShellyTerminalView`, `ReactNativeJS`).

1. Fresh launch: terminal prompt appears; `$HOME/bin/bash -c 'echo ok'`
   prints `ok` (not `No such file`). `readlink -f $HOME/bin/bash`
   resolves to `$libDir/shelly_shell`, and that file exists.
2. Soft keyboard: tap the terminal body, keyboard opens on the first
   tap. No `PHASE_CLIENT_APPLY_ANIMATION` cancel storm in logcat.
3. Paste: copy a multi-line block (3+ lines), press Paste, press Enter.
   Expect the pasted block to run as a single compound input, not
   line-by-line. Logcat shows one `ShellyPaste pasteViaEmulator` and
   the `ShellyPaste paste(...)` line with `bracketed=true`.
4. Claude Bash tool: `echo "hello from claude bash"`. Should succeed
   without `No suitable shell found`.
5. Gemini Bash tool: `echo "hello from gemini bash"`. Should succeed
   without `spawn bash ENOENT`.
6. Codex Bash tool: `pwd; whoami; uname -a`. Should succeed without
   `unable to open file "/bin/..."` errors.

### Still open (not touched this session)

- `#104` IME *height propagation* into pane padding. Fix #6 makes the
  keyboard actually appear; whether `MultiPaneContainer.keyboardHeight`
  is then applied correctly to the pane grid padding needs re-testing
  once the IME shows reliably. `Keyboard.metrics()` polling is already
  in place as a fallback. If still broken, candidate is replacing
  `Keyboard.addListener` + `Keyboard.metrics()` with
  `useAnimatedKeyboard()` from `react-native-reanimated` 4.
- Terminal-pane focus double-dispatch. `TerminalPane.focusedPaneId`
  effect and native `onSingleTapUp` both invoke
  `showKeyboardWhenServed` for the same tap. Fix #6 defuses the retry
  storm so both calls collapse to ~2 show requests, which IME can
  tolerate. Root-cause fix is to move IME-show to only `onSingleTapUp`
  (fromUser=true path) and leave `focusCommand` as pure `requestFocus`.
  Worth doing once fix #6 is verified on device.
- Codex `list_directory` missing the `$HOME/bin/bash` symlink entry
  that `glob` finds. Likely dangling-symlink hide in Node
  `fs.readdir`. After fix #4 the symlink won't be dangling, so the
  visibility discrepancy should also resolve. Re-check after build.

---

## 2026-04-24 Release gate — on-device smoke test before HN upload

**Do not upload this APK to GitHub Releases / announce to HN until
every step below passes on the physical Z Fold6.** All seven commits
are code-inspection-only fixes; none were device-verified. The
worst-case failure mode (LibExtractor mis-extracting
`libshelly_shell.so`) would leave the AI CLIs completely unstartable
— catastrophic for a launch.

### Build provenance

- Trigger run: https://github.com/RYOITABASHI/Shelly/actions/runs/24881389538
- Ref: `claude/friendly-goldwasser-392b6f`
- Tip commit: `7862da8b` (includes all 7 fixes listed above)
- Expected completion: ~30-35 min from `workflow_dispatch` time
- Artifact: `shelly-apk` (`android/app/build/outputs/apk/release/*.apk`)

Before installing, confirm the run completed successfully and the
artifact matches this ref. If the run fails, do not proceed — fix the
build error first. If a newer ref has landed in the meantime, rebuild
from that ref so the smoke test reflects the actual release candidate.

### Install on a clean-ish state

Uninstall the previous APK before installing. This matters because:

- `LibExtractor` gates re-extraction on a version marker, and we want a
  forced cold extract to verify `libshelly_shell.so -> $libDir/shelly_shell`
  actually lands on this ROM.
- `BASHRC_VERSION` bumped 51 -> 53, so `.bashrc` must regenerate and the
  `$HOME/bin/bash` symlink must be rewritten from scratch. Leaving the
  old dangling symlink in `$HOME/bin/` would mask the fix.

```powershell
adb uninstall dev.shelly.terminal
adb install path\to\shelly-release-*.apk
adb logcat -c
```

Open Shelly once so `HomeInitializer` runs (you will see `HomeInitializer:`
log lines while the shell PTY is launching). Wait for the prompt to appear
before starting the test sequence.

### Pre-flight sanity (verifies the fixes actually loaded)

In the Shelly terminal, run these four commands. All must succeed:

```bash
file $HOME/bin/bash
readlink -f $HOME/bin/bash
echo "$SHELL"
"$HOME/bin/bash" -c 'echo bash-wrapper-ok'
```

Expected output shape:

- `file` reports a symbolic link **to an existing file** (not `broken
  symbolic link`).
- `readlink -f` resolves to `$libDir/shelly_shell` where `$libDir` is
  `/data/data/dev.shelly.terminal/files/termux-libs`. The leaf must
  actually exist (`shelly_shell`, no `.so` extension).
- `$SHELL` prints the same path.
- Final command prints `bash-wrapper-ok` and exits 0.

If any of these fail, **stop**. The LibExtractor / bashrc path is
broken; releasing this APK will brick every AI CLI. Capture
`adb logcat -d -s LibExtractor:V HomeInitializer:V ShellyExecWrapper:V`
and route to fix-first, release-later.

### Smoke test matrix (stop at first failure)

Capture `adb logcat -d -s ShellyPaste:D ShellyIME:D ShellyExec:D
ShellyPTY:D HomeInitializer:D TerminalEmulator:D Sidebar:D Shelly:D
ShellyTerminalView:I ShellyExecWrapper:V ReactNativeJS:V` after each
block so failures can be diagnosed without reproducing.

| # | Step | Expected result | If it fails |
|---|------|-----------------|-------------|
| 1 | Launch Shelly from a fresh process. | Terminal prompt appears within a few seconds; `CommandKeyBar` is visible above where the keyboard will be. | `HomeInitializer` log line mentioning `libshelly_shell.so` missing -> extractor failed -> check APK contents. |
| 2 | Tap the terminal body. | Soft keyboard opens **on the first tap**. Logcat shows **one** `ShellyTerminalView: *.showKeyboard` line per gesture, no `PHASE_CLIENT_APPLY_ANIMATION` storm. | If keyboard stays hidden: confirm `WindowInsetsController.show(ime())` fallback fires, look for Samsung-specific "view is not served" lines, compare multi-pane vs single-pane. |
| 3 | Paste (Ctrl-V button) a three-line block, then press Enter. | The entire block executes as a single compound input, not line-by-line. Logcat shows one `ShellyPaste pasteViaEmulator` and one `ShellyPaste paste(...)` with `bracketed=true`. | If each line ran separately: `\C-x\C-b` bind missing from the current `.bashrc` -> verify `BASHRC_VERSION` file in `$HOME` shows `53`. |
| 4 | In bash, `claude "echo hello from claude bash"` (or equivalent CLI invocation). | CLI starts, executes `echo hello from claude bash`, prints `hello from claude bash`. **No** `No suitable shell found` / `spawn bash ENOENT`. | If `No suitable shell found`: `$SHELL` is not a valid POSIX name — confirm it resolves to something whose basename is `bash`. If `spawn bash ENOENT`: LibExtractor didn't land `shelly_shell` — see pre-flight step. |
| 5 | `gemini "echo hello from gemini bash"`. | Same as #4 for Gemini. | Same diagnostic lanes. `list_directory` should now show `bash` in `$HOME/bin/` (regression test for the earlier dangling-symlink visibility bug). |
| 6 | `codex "pwd; whoami; uname -a"`. | All three commands run without `unable to open file "/bin/..."`. | If any `/bin/...` ENOENT: `exec-wrapper.c` prefix rewrite not applied — double-check the APK has the updated `libexec_wrapper.so`. |
| 7 | Rotate to Z Fold6 unfolded (wide) layout, split into 2 panes, open IME on right pane. | Keyboard focuses the tapped pane only; left pane continues rendering. Each tapped pane shows one `showKeyboard` line. | If focus ping-pongs between panes: the `focusCommand` + `onSingleTapUp` double-dispatch we left open is biting — tolerable for release if #1-6 pass, but file as follow-up. |
| 8 | With keyboard open in split layout, check that the bottom prompt row plus `CommandKeyBar` stay visible above the IME. | Both rows above the keyboard; no content clipped under the IME. | If content clips: `#104` height-propagation follow-up (pane padding not shrinking to `keyboardHeight`). Document the residual, decide whether to release anyway based on severity. |

### Go / no-go criteria

- 1-6 all pass: **ship it.** 7 and 8 are nice-to-have polish; residual
  issues there are launch-tolerable.
- Any of 1-6 fails: **do not ship.** Capture logcat + `dumpsys
  input_method` + `adb shell ls -la $HOME/bin/` and start a new fix
  cycle. `docs/superpowers/DEFERRED.md` bug #104 entry and the "Still
  open" list above are the natural next candidates.

### Useful one-liners while running the gate

```powershell
# live tail of Shelly-only logs
adb logcat -s ShellyPaste:D ShellyIME:D ShellyExec:D ShellyPTY:D HomeInitializer:D ShellyTerminalView:I ShellyExecWrapper:V

# verify the current install's version and last-updated time
adb shell dumpsys package dev.shelly.terminal | findstr /i "versionName lastUpdateTime"

# verify libshelly_shell.so actually landed in $libDir
adb shell ls -la /data/data/dev.shelly.terminal/files/termux-libs/shelly_shell

# IME state (served view, frame rect, shown flag)
adb shell dumpsys input_method | findstr /i "mInputShown mServedView mServedInputConnection mImeFrame"
```

### Rollback plan if post-release regressions surface

Keep the previous release APK (versionName `4.2.0`, build
`lastUpdateTime 2026-04-24 08:34:05`) downloadable. The known-working
subset of the old APK is: basic terminal, Gemini echo, Codex recovers
after `/bin/sh` fallback. Claude Code Bash tool was broken there too,
so the rollback is not a full regression — just "different broken".
If 1-6 pass locally but users report breakage on other ROMs, triage
logs from affected devices before deciding between hotfix-forward and
rollback.

## 2026-04-24 Evening On-Device Test Results + Two Pushed Fixes

Smoke tested APK from run #687 (`7862da8b`) on Z Fold6 (Android 16,
SM-F956Q) via scrcpy. Two regressions found → fixed → pushed. Claude
Code Bash tool still broken (pre-existing, musl LD_PRELOAD limitation).

### Test summary

| # | Test | Result |
|---|------|--------|
| 1 | Basic terminal startup + `CommandKeyBar` visible above keyboard | PASS |
| 2 | MultiPane layout safety (p2 → p4 → p1 → p4 restores panes) | PASS |
| 3 | MultiPane focus/input: body-tap routes typing to tapped pane | **FAIL → fix `f947ad67` pushed** |
| 4 | Claude Code Paste button (multi-line) | **FAIL → fix `7c514138` pushed** |
| 5 | Claude Code Bash tool (`echo claude-bash-test`) | **FAIL, pre-existing** |

### Fix 1 — `f947ad67` cross-pane IME served view handoff

- Branch: `claude/friendly-goldwasser-392b6f`
- Files: `modules/terminal-view/android/.../ShellyTerminalView.kt`

`c236349f` dropped the retry ladder to stop an Android 15 cancel storm,
which also killed the only case where a retry is genuinely necessary:
cross-pane focus handoff. When a sibling `TerminalView` was previously
served, `IMM.focusIn(newView)` doesn't fire until `ViewRootImpl`
processes the focus traversal on the next Choreographer pass. Until
then `showSoftInput()` is rejected with "view is not served" and the
keyboard stays on the old pane. Tapping the new pane again didn't help
because both `mServedView` and `mNextServedView` remained pointed at
the old pane.

Confirmed on-device:

```text
onSingleTapUp sess=shelly-1 reqFocus=true hasWin=true focused=true
   imeShow=false prevFocusWas=TerminalView#4543993 viewHash=93054751
Ignoring showSoftInput() as view={58be71f} is not served
mServedView=TerminalView{4555f9}
mNextServedView=TerminalView{4555f9}
```

Fix adds exactly one next-frame retry inside `showKeyboardWhenServed`,
guarded by `rootView.findFocus() === terminalView` to skip the retry
if the view has since lost focus. Preserves the cancel-storm fix (no
5-step `postDelayed` ladder, no `fromUser=false` storm).

### Fix 2 — `7c514138` widen paste TUI detection

- Branch: `claude/friendly-goldwasser-392b6f`
- Files: `modules/terminal-emulator/android/.../TerminalEmulator.java`

`ecef1cb6` dropped the `multiLine` branch because it broke bash
readline paste on bionic (ESC in `\e[200~` swallowed at dispatch,
`[200~` leaked as literal, each `\n` executed a separate command).
That fix was correct for bash but over-narrow for Claude Code / Codex
/ Gemini: they advertise DECSET 2004 but never enter alt-buffer or
mouse tracking at their input prompts, so `tuiMode=false` →
readline-only path picked → `\x18\x02` + payload + `\e[201~` → TUI
drops `\x18\x02` as garbage, each `\n` submits current line, multi-
line paste shatters.

Confirmed on-device:

```text
D/ShellyPaste: paste(raw=36, sanitized=35, nl=1, bracketed=true,
  tui=false, preview="paste-test line 1↵paste-test lin…")
```

Fix widens `tuiMode` to also include DECSET 1 (application cursor
keys) and DECSET 66 (application keypad). Every full TUI reading
arrow keys via terminfo `smkx` sets at least one of these. Bash
readline at a normal prompt does NOT, so the `\C-x\C-b` path it
depends on stays intact.

### Known pre-existing issue — Claude Code Bash tool `Exit code 1`

**Not fixed in this session**. Root cause characterized:

From Shelly's interactive bash (PID of `libbash.so` loaded via
linker64):
- `SHELL=/data/user/0/.../home/bin/bash`
- `BASH=/data/user/0/.../termux-libs/shelly_shell`
- `LD_PRELOAD=/data/user/0/.../termux-libs/libexec_wrapper.so`
- `~/bin/bash → ~/termux-libs/shelly_shell`
- `~/bin/sh → /system/bin/sh`
- `~/bin/env → /system/bin/env`
- `bash -c 'echo direct-bash-test'` **works** (wrapper catches exec calls)

From Claude Code (musl Bun SEA, PID is child of Shelly bash):
- Claude spawns `child_process.spawn(cmd, { shell: true })` which
  internally invokes `/bin/sh -c cmd` under Node semantics.
- Claude's process is linked against **musl** (not bionic), and
  `shelly_musl_exec` trampoline strips bionic env that musl cannot
  use. Even if `LD_PRELOAD` were inherited, `libexec_wrapper.so` is
  bionic-linked so musl's dynamic linker can't load it.
- Consequence: the `/bin/sh → /system/bin/sh` rewrite is **not
  applied** inside Claude's process. `/bin/sh` literally doesn't
  exist on Android → `execve("/bin/sh", …)` returns `ENOENT` →
  Node's spawn error handler surfaces exit 1 with no stdout/stderr.

This matches bug #117 Path C scope (custom musl libc) and the Codex
precedent from the 2026-04-23 addendum (`error: unable to open file
"/bin/sh"`).

### Options for the Claude / Codex shell-spawn fix

**A. Musl-compiled wrapper (30–60 min)**
Recompile `libexec_wrapper.c` against an aarch64-musl toolchain as
`libexec_wrapper_musl.so`. Have `shelly_musl_exec` inject
`LD_PRELOAD=libexec_wrapper_musl.so` into the musl env before handing
control to Claude's interpreter. Risk: no musl toolchain on the build
image yet; compile may need extra CI wiring.

**B. Patch Claude's `cli.js` post-install (10–15 min)**
Extend the existing `__shelly_bg_cli_update` pipeline to sed-replace
`{ shell: true }` → `{ shell: '/system/bin/sh' }` (or explicit
`'/data/.../home/bin/bash'`) in
`$PREFIX/lib/node_modules/@anthropic-ai/claude-code/cli.js`. Reapplies
on every Claude update because the updater runs daily. Same
technique works for `@openai/codex`. Risk: brittle against upstream
refactors that drop the literal `shell: true` string.

**C. Defer to bug #117 Path C**
Continue the custom-musl-libc track in
`2026-04-21-bug117-path-c-handoff.md`. Slowest but most complete.

Default recommendation: **Option B** first (ships quickly, unblocks
HN day-1 users who install Claude Code / Codex). Option A in parallel
for durability. Option C remains the architectural north star.

### Build status at end of session

- `7c514138` is the tip of `claude/friendly-goldwasser-392b6f`.
- Local APK installed at test time: run #687 / `7862da8b`. Does NOT
  contain `f947ad67` or `7c514138` yet — those fixes must be verified
  on a fresh build.
- Next-session action: wait for the new GitHub Actions run for
  `7c514138`, install, re-run Tests 3 and 4. If both PASS and
  Options B/C are not yet picked, HN-post minimum (Tests 1, 2, 3, 4)
  is green; ship with Claude Bash tool documented as known limitation.

## 2026-04-24 Late Night — Second Stabilization Pass

Build 688 installed and smoke-tested on Z Fold6. Two confirmed
regressions (Tests 3 and 4 both FAIL, plus Bash tool + cosmetic
leaks still pre-existing). Multi-agent review/debug triggered a
second round of fixes covering paste routing, IME handoff, CLI
subprocess spawning, and pane focus/input pipeline. All work lives
on `claude/friendly-goldwasser-392b6f` (also mirrored to
`claude/stoic-hugle-569bef`).

### Session build history

| Run | SHA | Status | Content |
|-----|-----|--------|---------|
| 688 | `7862da8b` | ✅ | Prior tip — Tests 3 / 4 FAIL, Bash tool FAIL |
| 689 | `83f61542` | ✅ | IME v1 + Option B + TMPDIR + echo gate (**no paste v2**) |
| 690 | `f4df524d` | ⚠️ cancelled (compile error caught in review) |
| 691 | `e0ebea1d` | cancelled (superseded) |
| 692 | `bd3961ed` | cancelled (superseded) |
| 693 | `a21570bb` | queued | **All fixes.** Target release build. |

### Fixes on `a21570bb` (commits `a4431c78..a21570bb`)

Group 1 — IME / text input
- `96414dfc` force IMM `mServedView` rebind via `hide+show` cycle
  with single 80 ms `postDelayed` retry. Addresses Samsung OneUI
  stale-servedView on cross-pane focus.
- `a21570bb` P0-1/2/3 + P1-1/2/3 + P2-1 pane fixes:
  - `showKeyboardWhenServed` 50 ms debounce (stops triple-dispatch).
  - `onSingleTapUp` splits clearFocus/requestFocus across ViewRoot
    passes via `post{}` so OneUI's focus state machine sees both.
  - `onWindowFocusChanged` gates `imm.restartInput` on `isFocused()`
    so multi-pane modal dismiss doesn't storm IMM.
  - `doRemoveBySlot` / `doAddPane` mirror focus into `usePaneStore`
    so `focusedPaneId` follows the slot lifecycle.
  - `destroy()` calls `hideSoftInputFromWindow` to drop IMM refs.
  - Unique `View.generateViewId()` per TerminalView (distinct
    `aid=` in logcat).

Group 2 — Paste routing
- `f4df524d` rewrites `TerminalEmulator.paste()` to consult
  `/proc/<bashPid>/stat` field 8 (tpgid) instead of DECSET bits.
  Claude / Codex / Gemini only set DECSET 2004 so the old heuristic
  misrouted their paste through the `\C-x\C-b` readline trigger.
- `e0ebea1d` compile fix (local `bashForeground` var was dropped
  in the refactor — caught by code-reviewer agent before build
  693 wasted CI).
- `87010830` comm-check guards — reject when `/proc/<shellPid>/comm`
  isn't `bash` (zsh/fish regression), and accept when
  `/proc/<tpgid>/comm` is `bash` (nested bash with inherited bind).

Group 3 — CLI subprocess integration
- `83f61542` three-part polish pass:
  - **Option B** — `shelly-patcher.js` gains `patchClaude()` that
    sed-replaces minified `shell:!0` → `shell:"/system/bin/sh"` in
    cli.js (5 call sites). Mirrored in CI against the APK-bundled
    tree so the fix is live on first launch.
  - TMPDIR = `$HOME/tmp` export so Gemini's
    `os.tmpdir()`-based `shell_pgrep_*.tmp` tracker lands in a
    writable dir (our bundled libnode.so hardcodes the Termux
    path as the fallback).
  - `[ -t 2 ]` gate on the `[shelly] background CLI update
    started` echo so it doesn't leak into Codex/Gemini/Claude
    shell tool output.
- `bd3961ed` Gemini subprocess-audit fixes:
  - `patchGemini()` extended (runtime) + CI mirror sed passes:
  - `shell:\s*true` → `shell:"/system/bin/sh"` (auto-update,
    GrepTool pre-flight, sandbox proxy).
  - `execSync(getCommandExistsCmd(cmd), { stdio: "ignore" })`
    → `{ stdio: "ignore", shell: "/system/bin/sh" }` so editor
    detection (vim/nano/cursor) stops silently returning false.
- Codex JS layer audited and confirmed clean — `bin/codex.js`
  has a single `spawn(binaryPath, argv, {stdio, env})` with no
  `shell` option, and the native binary is already redirected
  via `shelly-patcher.js`.

### Agent-assisted review/debug stack

Seven agent runs contributed to the above fix set — record the
chain so future sessions don't re-run the same audits:

1. `superpowers:code-reviewer` on 3-commit stack →
   caught `bashForeground` compile error (saved one build cycle).
2. General-purpose: IME stuck served view canonical patterns →
   confirmed v4 hide+show+postDelayed is defensible; documented
   Samsung OneUI resize-time re-stomp.
3. General-purpose: Claude paste edge cases (10 scenarios) →
   flagged zsh/fish regression and nested-bash regression →
   comm guards landed.
4. General-purpose: cross-CLI subprocess audit (Claude / Codex /
   Gemini) → flagged 3 Gemini paths (P0 commandExists + P1
   auto-update + P1 GrepTool), Claude & Codex clean.
5. `/security-review` skill → no HIGH / MEDIUM vulns.
6. General-purpose: pane focus + text-input pipeline audit →
   flagged 3 P0 (triple-dispatch, HW keyboard split-pass,
   restartInput storm) + 4 P1 (pane-lifecycle focus sync, tab
   switch IC reset, fold/unfold 250 ms polling race) + 2 P2
   (View.NO_ID, paste session binding).
7. (This session) second `superpowers:code-reviewer` targeted at
   combined paste+IME+CLI work — pending for build 693 if any
   third-pass review is needed before release.

### Release gate for build 693

If the APK builds successfully, install and run this exact test
matrix. PASS on all lines is the ship criterion.

| # | Test | Expected |
|---|------|----------|
| 1 | Basic terminal + CommandKeyBar visible above keyboard | PASS |
| 2 | MultiPane layout safety (p2 → p4 → p1 → p4) | PASS |
| 3 | MultiPane focus — tap L pane → type → tap R pane → type | PASS (both panes receive correct input) |
| 4 | Claude Code Paste button (2-line payload) | Both lines arrive in Claude's input field, no `[200~` literal, no submit |
| 5 | Claude Bash tool `echo claude-bash-test` | Outputs `claude-bash-test`, exit code 0 |
| 6 | Gemini `!echo gemini-shell-test` (shell tool) | Output without `shell_pgrep_*.tmp` stderr leak |
| 7 | Codex cold-start after 24h inactivity | No `[shelly] background CLI update started` in tool output |
| 8 | Gemini `/edit somefile.txt` | Editor actually launches (commandExists fix) |
| 9 | HW keyboard / scrcpy typing into each of 2 panes | Input routes correctly after focus switch (no "PC input dead") |
| 10 | PTY paste while zsh is `$SHELL` | No `^X^B` leak (comm guard) |

Go/no-go:
- Tests 1-5 PASS → basic release viable
- Tests 6-8 PASS → full CLI integration
- Tests 9-10 PASS → full keyboard handling

If any P0/P1 test regresses, logcat tags to inspect:
`ShellyPaste:D ShellyIME:D ShellyTerminalView:D Keyboard:D MultiPane:D HomeInitializer:D`.

### Next-session starting point

- `gh auth token` is populated in the Windows gh CLI; use
  `curl -H "Authorization: Bearer $(gh auth token)"` to dispatch
  workflows from this session directly (verified working).
- Branch tip: `a21570bb` on both `claude/friendly-goldwasser-392b6f`
  and `claude/stoic-hugle-569bef`. If build 693 PASSes the test
  matrix above, fast-forward merge into `main` and cut release.
- P1-4 (250 ms fold/unfold polling race) and P2-2 (paste→wrong
  session when focusedPaneId≠activeSessionId) intentionally
  deferred — both rare, neither observed during today's tests.
  Move to DEFERRED.md as P1/P2 before release.

## 2026-04-24 Late-Late Night — Third Stabilization Pass (builds 693 → 697)

Builds 693 and 694 installed and on-device tested on Z Fold6
(Android 16 / OneUI 6). Test 5 / 6 / 7 / 8 PASSed on 693. Test 3
and Test 4 both FAILed on 694 — the IME WeakRef tracker was
correctly detecting cross-pane handoff but `clearFocus()` on the
stale sibling was a no-op, and paste into Claude Code routed
through bash's `\C-x\C-b` bind because `bashFg=true` fired even
when Claude was the foreground process. Two parallel agents were
dispatched during the wait for build 696 to root-cause both.

### Build history (this pass)

| Run | SHA | Status | Delta |
|-----|-----|--------|-------|
| 693 | `a21570bb` | ✅ | Install verified; Tests 5 / 6 / 7 / 8 PASS, Test 3 FAIL |
| 694 | `3adc8a68` | ✅ | Install verified; Tests 3 / 4 still FAIL |
| 695 | `5c4d79ba` | cancelled | Superseded before build completed |
| 696 | `397382a2` | ⚠️ FAIL | Compile error: `focusOut` unresolved reference |
| 697 | `3370c422` | queued | **Target release build with agent-verified fixes** |

### Test outcomes on build 693

| # | Test | Result |
|---|------|--------|
| 5 | Claude Bash tool | **PASS** only with `SHELLY_FORCE_LEGACY_CLAUDE=1` |
| 6 | Gemini `!echo gemini-shell-test` | **PASS** — no pgrep stderr leak |
| 7 | Codex cold start | **PASS** — no `[shelly] background CLI update` echo |
| 8 | Gemini `vim --version` via shell tool | **PASS** — vim version returned |
| 3 | MultiPane focus tap | **FAIL** — keyboard stuck on previous pane |
| 4 | Claude paste 2-line | **FAIL** — paste corrupts because bashFg=true |

### Critical discovery 1 — Option B is cli.js-only (not musl Bun SEA)

Build 693 `claude` (default path) runs the musl Bun SEA
(`$libDir/claude` via `ld-musl-aarch64.so.1`). That's claude-code
2.1.118 shipped as a pre-compiled ELF — Option B's `shell:!0`
sed only touches cli.js, so the SEA still hits `/bin/sh` and
`Exit code 1`s. Verified on-device:

```
default claude:                       Bash tool → Exit code 1
SHELLY_FORCE_LEGACY_CLAUDE=1 claude:  Bash tool → "claude-bash-test" ✓
```

Fix (`5c4d79ba`): swap `claude()` priority so cli.js 2.1.112
(Option B patched) is the default, musl Bun SEA is opt-in via
`SHELLY_PREFER_MUSL_CLAUDE=1`. Tradeoff: pinned to 2.1.112 unless
user opts in.

### Critical discovery 2 — mServedView stuck on stale sibling

On-device `adb shell dumpsys input_method` after tapping sibling
pane showed:

```
mServedView=TerminalView{1d30853 ... aid=1073741826}   ← OLD pane
[current focused view has viewHash 226158926]
```

Mechanism traced:
1. Termux's internal `TerminalView.onSingleTapUp` (line 215) calls
   `requestFocus()` BEFORE our `ShellyTerminalView.onSingleTapUp`.
2. Our handler's `rootView.findFocus()` therefore returns the
   NEW view, not the stale sibling — findFocus-based detection
   from `a21570bb` never fired (logcat confirmed zero
   `onSingleTapUp.post` entries).
3. `3adc8a68` replaced findFocus with a process-wide
   `WeakReference<TerminalView>` tracking last-tapped view —
   `onSingleTapUp.post` started firing on the correct code path.
4. But `prevTerminalView.clearFocus()` inside the post path is
   still a NO-OP, because Termux's upstream `requestFocus()`
   already cleared sibling's `PFLAG_FOCUSED`.
5. `View.clearFocus()` short-circuits when `PFLAG_FOCUSED == 0`,
   so `onFocusChanged(false)` never dispatches → IMM never sees
   `focusOut` for the stale sibling → `mServedView` stays stuck.

### Critical discovery 3 — bash job_control=0 on Shelly's bionic bash

Paste agent root-caused `bashFg=true` during Claude sessions:
Shelly's bash (libbash.so loaded via linker64) fails
`initialize_job_control()` because SELinux denies `/dev/tty`
access. Bash silently sets `job_control=0` and stops calling
`tcsetpgrp()` for foreground commands. Forked children (Claude,
Codex, Gemini, vim) inherit bash's pgid → `/proc/<bashPid>/stat`
field 8 (tpgid) stays at shellPid → my earlier tpgid-based check
returned `true` even when a CLI owned the TTY.

### Agent-verified fixes (in build 697)

**IME** — agent flagged that `InputMethodManager.focusOut(View)`
is `@hide` since API 30, on the greylist-max-p list, and isn't
in `android.jar` stubs (hence build 696 compile failure). The
correct public API is:

```kotlin
imm.onViewFocusChanged(prevTerminalView, /*hasFocus=*/ false)
```

Added in API 34 (Android 14) exactly for this IME-rebind
scenario. Z Fold6 is API 36 so the branch always fires.
`onViewFocusChanged` internally updates IMM's bookkeeping AND
dispatches focusOut to the bound IME, resetting `mServedView`.

**Paste** — agent recommended `tcgetpgrp(pty_master_fd)` via
JNI as the kernel-authoritative signal (unaffected by bash's
job-control state). JNI wiring deferred; build 697 ships the
agent's fallback approach instead:

```java
// /proc/<shellPid>/task/<shellPid>/children lists bash's
// immediate forked children regardless of job-control state.
// Empty → bash at prompt. Non-bash child → CLI running.
```

The children-file approach handles every case the agent
identified and requires zero JNI changes.

### Code-review trail

Agents used this session:
1. `superpowers:code-reviewer` on the 3-commit (→a21570bb)
   stack — caught compile error before 692 shipped.
2. General-purpose IME-canonical-pattern agent — confirmed
   WeakRef approach directionally correct, flagged activity-
   recreate edge case (deferred to DEFERRED.md).
3. General-purpose paste edge-case agent — caught zsh/fish
   regression + nested-bash regression, comm guards added
   (`87010830`).
4. General-purpose cross-CLI subprocess audit — flagged 3
   Gemini paths (`bd3961ed`); Claude & Codex clean.
5. `superpowers:security-review` — no HIGH/MEDIUM vulns.
6. General-purpose pane-focus pipeline agent — P0-1/2/3,
   P1-1/2/3, P2-1 fixes (`a21570bb`).
7. `superpowers:code-reviewer` on 3adc8a68 — flagged 2 edge
   cases, both deferred as P1 follow-ups.
8. **[this round]** General-purpose IME-focusOut-verifier —
   caught that `focusOut` is `@hide`, recommended
   `onViewFocusChanged`. Saved build 697 from repeating the
   690-style wasted cycle.
9. **[this round]** General-purpose paste-tpgid root-cause
   agent — identified bash job_control=0 mechanism, proposed
   both tcgetpgrp-JNI and children-file fallback.

### Release gate for build 697

If 697 PASSes the matrix below, fast-forward
`claude/friendly-goldwasser-392b6f` into `main` and cut the HN
release.

| # | Test | Expected |
|---|------|----------|
| 1 | Basic terminal + CommandKeyBar | PASS |
| 2 | MultiPane layout safety | PASS |
| 3 | MultiPane focus tap | **Keyboard moves to tapped pane, typing lands there** |
| 4 | Claude paste 2-line | **2 lines in Claude input, no `[200~` literal** |
| 5 | Claude Bash tool `echo claude-bash-test` | Output `claude-bash-test` (cli.js default now) |
| 6 | Gemini `!echo gemini-shell-test` | Clean output, no pgrep leak |
| 7 | Codex cold start | No `[shelly]` echo leak |
| 8 | Gemini `vim --version` | Version info returned |
| 9 | scrcpy PC keyboard into each pane after switch | Correct pane receives input |
| 10 | Paste into zsh as `$SHELL` | No `^X^B` leak (comm guard) |

logcat filters for 697 diagnostics if anything regresses:
```
adb logcat -s ShellyPaste:D ShellyIME:D ShellyTerminalView:D \
  Keyboard:D MultiPane:D HomeInitializer:D InputMethodManager:W
```

For IME specifically, look for:
- `onSingleTapUp.post` entries → WeakRef detected handoff ✓
- `onViewFocusChanged skipped: ...` → API-34 branch didn't fire
  (shouldn't happen on Android 16 but logs it if it does)
- `dumpsys input_method | grep mServedView` → should update
  to the tapped view after a tap, not stay on the sibling

For paste, look for:
- `bashFg=false` when tapping paste inside Claude/Codex/Gemini/vim
- `bashFg=true` only at bash prompt

### Deferred (post-release)

Before cutting release, move these to `docs/superpowers/DEFERRED.md`
as P1 / P2 entries:

1. **JNI tcgetpgrp for paste routing** (P1) — agent's recommended
   primary signal. `/proc/children` fallback is sufficient for
   today's use cases but tcgetpgrp is the kernel-canonical answer.
2. **WeakRef tracker edge cases** (P1) — activity recreate
   (Fold/unfold) leaves tracker pointing at detached corpse;
   programmatic `requestFocus()` (React autofocus) bypasses
   `onSingleTapUp` so tracker doesn't seed. Mitigate via
   `onAttachedToWindow` / `onWindowFocusChanged` hook that
   refreshes the tracker whenever this view gains focus.
3. **P1-4 / P2-2** from the prior pane-focus audit remain
   deferred as before.

### Next-session starting point

- Branch tip at release gate: `3370c422` on both
  `claude/friendly-goldwasser-392b6f` and
  `claude/stoic-hugle-569bef`.
- Build 697 run id `24898374896`, dispatched at 23:46 JST
  2026-04-24 (Z Fold6 local). ETA ~25 min.
- `gh auth token` populated, dispatch + artifact downloads
  work from this session directly (confirmed during this pass).
- If 697 passes the matrix: `git push origin
  claude/friendly-goldwasser-392b6f:main`, cut a GitHub Release
  with the 421 MB `shelly-apk` artifact, draft HN post.
- If any row regresses: logcat tags above + `adb shell dumpsys
  input_method` are the fastest paths to the next diagnosis.

